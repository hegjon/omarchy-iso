# Configuration for archiso with Plymouth
# omarchy-start-banner must come after plymouth: its install hook overrides
# the theme script plymouth added (add_file overwrites, last hook wins).
HOOKS=(base udev microcode modconf kms memdisk archiso archiso_loop_mnt archiso_pxe_common archiso_pxe_nbd archiso_pxe_http archiso_pxe_nfs plymouth omarchy-start-banner block filesystems keyboard)
