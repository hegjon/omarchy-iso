#!/bin/bash

build() {
    add_runscript
}

help() {
    cat <<HELPEOF
Prints a dim 'Starting the installer...' line on the console as soon as the
initramfs starts, covering the seconds before Plymouth's splash can draw.
Purely cosmetic; adds nothing else to the image.
HELPEOF
}
